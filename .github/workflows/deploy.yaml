name: Deploy

on: 
  workflow_run:
    workflows:
      - "CI"
    branches:
      - "main"
      - "master"
    types:
      - completed

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - run: npm run deploy
